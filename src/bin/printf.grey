#!/usr/bin/env bash
#
# Description: outputs the given arguments in grey color.
# Usage: printf.grey [options]
#

set -euo pipefail

readonly STARTCOLOR=$'\e[1;90m'
readonly ENDCOLOR=$'\e[0m'

echo -n "${STARTCOLOR}"
# shellcheck disable=SC2059
printf "$@"
echo -n "${ENDCOLOR}"
