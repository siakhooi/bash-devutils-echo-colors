#!/usr/bin/env bash
#
# Description: outputs the given arguments in blue color.
# Usage: printf.blue [options]
#

set -euo pipefail

readonly STARTCOLOR=$'\e[1;34m'
readonly ENDCOLOR=$'\e[0m'

echo -n "${STARTCOLOR}"
# shellcheck disable=SC2059
printf "$@"
echo -n "${ENDCOLOR}"
