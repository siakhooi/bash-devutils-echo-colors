#!/usr/bin/env bash
#
# Description: outputs the given arguments in magenta color.
# Usage: printf.magenta [options]
#

set -euo pipefail

readonly STARTCOLOR=$'\e[1;35m'
readonly ENDCOLOR=$'\e[0m'

echo -n "${STARTCOLOR}"
# shellcheck disable=SC2059
printf "$@"
echo -n "${ENDCOLOR}"
