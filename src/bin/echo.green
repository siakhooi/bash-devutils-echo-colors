#!/usr/bin/env bash
#
# Description: outputs the given arguments in green color.
# Usage: echo.green [options]
#

set -euo pipefail

readonly STARTCOLOR=$'\e[1;32m'
readonly ENDCOLOR=$'\e[0m'

echo -n "${STARTCOLOR}"
echo "$@"
echo -n "${ENDCOLOR}"
