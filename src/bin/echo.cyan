#!/usr/bin/env bash
#
# Description: outputs the given arguments in cyan color.
# Usage: echo.cyan [options]
#

set -euo pipefail

readonly STARTCOLOR=$'\e[1;36m'
readonly ENDCOLOR=$'\e[0m'

echo -n "${STARTCOLOR}"
echo "$@"
echo -n "${ENDCOLOR}"
