#!/usr/bin/env bash
#
# Description: outputs the given arguments in yellow color.
# Usage: printf.yellow [options]
#

set -euo pipefail

readonly STARTCOLOR=$'\e[1;33m'
readonly ENDCOLOR=$'\e[0m'

echo -n "${STARTCOLOR}"
# shellcheck disable=SC2059
printf "$@"
echo -n "${ENDCOLOR}"
